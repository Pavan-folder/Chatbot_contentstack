import React from 'react';
import ReactDOM from 'react-dom/client';
import AppTravelMate from './AppTravelMate';

const root = ReactDOM.createRoot(
  document.getElementById('root') as HTMLElement
);

root.render(
  <React.StrictMode>
    <AppTravelMate />
  </React.StrictMode>
);
